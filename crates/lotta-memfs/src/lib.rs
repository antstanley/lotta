//! Git-backed memory repositories and prompt-input adapters.
//!
//! This crate implements memory filesystem ports. It must not depend on transport adapters or
//! other concrete adapters.

#![forbid(unsafe_code)]
#![deny(missing_docs)]
